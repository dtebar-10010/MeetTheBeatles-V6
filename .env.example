# Example environment variables for production (DO NOT commit real secrets to the repo)
# Copy this to .env (or set the variables in your hosting provider / PythonAnywhere Web tab)

# Security
SECRET_KEY=replace-with-a-long-random-secret
DJANGO_DEBUG=False
ALLOWED_HOSTS=yourusername.pythonanywhere.com

# Database (optional - if using an external DB)
# DATABASE_URL=postgres://USER:PASSWORD@HOST:PORT/NAME

# HTTPS / cookie settings
SECURE_SSL_REDIRECT=True
SESSION_COOKIE_SECURE=True
CSRF_COOKIE_SECURE=True
SECURE_HSTS_SECONDS=3600
